const rows = 11;
const cols = 11;

const vrows = 5;
const vcols = 5;

const lw = 10;
const celltimeout = 10;

const limit = 40;

const maxplayers = 100;
const port = 82;

module.exports = {
    rows: rows,
    cols: cols,
    maxplayers: maxplayers,
    vrows: vrows,
    vcols: vcols,
    lw: lw,
    limit: limit,
    celltimeout: celltimeout,
    port: port
};
