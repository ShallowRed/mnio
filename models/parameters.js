const rows = 250;
const cols = 250;
const vrows = 20;
const vcols = 20;

const lw = 10;
const limit = 40;
const celltimeout = 10;

const port = 82;

module.exports = {
    rows: rows,
    cols: cols,
    vrows: vrows,
    vcols: vcols,
    lw: lw,
    limit: limit,
    celltimeout: celltimeout,
    port: port
};
